# Desktop wrapper

```bash
npm install
npm start
npm run dist
```

The Electron wrapper packages the generated PWA for Windows, macOS and Linux. Production code signing/notarization is intentionally left to the distributor rather than embedding credentials in the repository.
