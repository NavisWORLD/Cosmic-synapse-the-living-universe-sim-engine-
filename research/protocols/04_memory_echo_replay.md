# Protocol 04 — Memory Echo invariants

**Status: PROPOSED TEST — NOT YET RUN**

Before capture, declare invariants such as world seed, object/event counts, selected state checkpoints and player path checkpoints. Export/capture a Memory Echo, reload it, and calculate preservation rates.

A visually compelling result is not sufficient; the predeclared invariants determine success.
